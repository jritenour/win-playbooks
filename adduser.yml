---
- hosts: all
  tasks:
    - name: Ensure user bob is present
      win_user:
        name: '{{ username }} '
        password: '{{ password }}'
        state: present
        groups:
          - Users
